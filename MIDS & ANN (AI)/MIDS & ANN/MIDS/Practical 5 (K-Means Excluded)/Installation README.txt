## For VS Code Terminal / CMD

Install required libraries using:

```bash
pip install numpy matplotlib
```

If `pip` does not work, use:

```bash
python -m pip install numpy matplotlib
```

---

## For Anaconda Prompt / Anaconda Package Manager

Install required packages using:

```bash
conda install numpy matplotlib
```

If asked:

```text
Proceed ([y]/n)?
```

Type:

```text
y
```

and press Enter.

---

## Verify Installation (Optional)

Run this command:

```bash
python
```

Then test imports:

```python
import numpy
import matplotlib
```

If no error appears, installation is successful.

Exit Python using:

```python
exit()
```

---

## Libraries Used in This Practical

* NumPy → Mathematical calculations and centroid computation
* Matplotlib → Cluster visualization and graph plotting
