Before running the practical, install the required Python libraries using this command:

```bash id="6yj4lm"
pip install pandas numpy matplotlib seaborn scikit-learn
```

If you are using Anaconda Prompt, you can also use:

```bash id="qitdsv"
conda install pandas numpy matplotlib seaborn scikit-learn
```

---

# Recommended Practical Execution Order

## For `.py` File

```bash id="ojst8p"
python Titanic_Preprocessing.py
```

---

## For Jupyter Notebook (`.ipynb`)

Start Jupyter Notebook using:

```bash id="r9wkmp"
jupyter notebook
```

Then open:

```text
Efficient_Titanic_Preprocessing.ipynb
```

and run cells using:

```text
Shift + Enter
```

---

# Optional (Best Practice)

Check installed libraries:

```bash id="5uqj36"
pip list
```

or

```bash id="qg9ot7"
conda list
```

This helps during practical exams if the examiner asks whether packages are installed correctly.
